function CLI(request : dval) {
    var db := sqlite_connect("/missing-capy-sqlite-parent/database.sqlite")
    sqlite_query(db, "select 1")
    print(contains(string(sqlite_info(db, "error"), ""), "without an open connection"), "|", arc_live())
    sqlite_disconnect(db)
}
