function RENDER(request : dval) {
    request.out.status = 404
    print("bearer-unit-cli")
}

function CLI(request : dval) {
    request.out.headers["Content-Type"] = "text/plain; charset=utf-8"
    print("hello-cli\n")
    print("fallback\n")
}
