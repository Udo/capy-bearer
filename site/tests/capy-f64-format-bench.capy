function CLI(request : dval) {
    var n := 1000
    var t0 := time_precise()
    var a := 0
    for i := 0..n { a = a + length(string(f64(i) + 0.1)) }
    var t1 := time_precise()
    print((t1 - t0) * 1000000.0 / f64(n), " us/op\n")
    print(a, "\n")
}
