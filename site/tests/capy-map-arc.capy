function CLI {
    {
        var mapped := map(dval(["beta", "alpha"]), function(value : string) string { return to_upper(value) })
        print(mapped[0], "|", mapped[1], "|")
    }
    print(arc_live())
}
