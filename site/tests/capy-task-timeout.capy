function CLI {
    var id := task("/tests/task-handlers.capy:SLOW", dval(""))
    var status := task_await(id, 5000u64)
    print(dval_to_string(status["state"]), ":", dval_to_string(status["failure_code"]))
}
