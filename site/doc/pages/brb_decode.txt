:sig
DValue brb_decode(String encoded)
bool brb_decode(String encoded, DValue& out, String* error_out = 0)

:see
brb_encode
0_DValue
json_decode

:content
Decodes BRRB1 bytes produced by `brb_encode()` back into a `DValue`.

The one-argument form returns an empty `DValue` on invalid input. The three-argument form reports whether decoding succeeded and can return a human-readable error string.


:example
DValue original; original["n"] = "7";
DValue restored = brb_decode(brb_encode(original));
print(restored["n"].to_string(), "\n");
