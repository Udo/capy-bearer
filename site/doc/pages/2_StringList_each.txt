:sig
DValue::each(String callback) (legacy `StringList` page)

:content
Removed as a separate StringList API. List-shaped `DValue::each()` invokes the callback once per string in numeric list order.

:see
0_DValue
2_DValue_each
