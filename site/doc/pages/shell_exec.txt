:sig
function shell_exec(command : string) string

:returns
Command output.

:content
Use `shell_exec` to run a shell command and get its output. Escape external values with `shell_escape` before you add them to the command.

:example capy render
print(shell_exec("printf hello"), "\n")
