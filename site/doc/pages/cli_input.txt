:sig
`cli_input` is not available in Capy.

:content
Capy does not provide the retired CLI input reader.

In a `CLI` handler, read `request.query`, `request.form`, and `request.body` directly. Each handler receives one copied `request : dval` value.

:see
>1_CLI
>0_Request
cli_arg
json_decode
