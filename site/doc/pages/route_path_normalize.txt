:sig
function route_path_normalize(path : string) string

:params
path : route text to normalize

:returns
Normalized route text.

:content
This function removes outer whitespace and leading and trailing slashes. It does not validate the result.

:example capy render
print(route_path_normalize(" /docs/api/ "), "\n")
