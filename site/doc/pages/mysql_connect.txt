:sig
function mysql_connect(host : string, username : string, password : string, database : string) u64

:params
host : the MySQL server host
username : the MySQL user name
password : the MySQL password
database : the initial database name

:returns
a request-local MySQL connection handle

:content
`mysql_connect()` creates a MySQL connection lease and returns its handle.

Call `mysql_connected()` before database work. Call `mysql_disconnect()` after use. A failed connection can return a nonzero handle.

:example capy render
var database := mysql_connect("localhost", "root", "", "")
if mysql_connected(database) { print("connected\n") } else { print(mysql_error(database), "\n") }
if database != u64(0) { mysql_disconnect(database) }

:see
>mysql
mysql_connected
mysql_disconnect
mysql_error
