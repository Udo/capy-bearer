:sig
function sha256_hex(value : string) string

:returns
Lowercase hexadecimal SHA-256 digest.

:content
Use `sha256_hex` to create a printable SHA-256 digest.

:example capy render
print(sha256_hex("abc"), "\n")
