:sig
function ob_get() string

:returns
the current output buffer contents

:content
`ob_get()` returns the current output buffer contents.

The buffer stays active after this call.

:example capy render
ob_start()
print("partial")
var output := ob_get()
ob_close()
print(output, "\n")

:see
>ob
ob_start
ob_get_close
