:title
to_upper

:sig
String to_upper(String s)

:params
s : C++ text to convert
return value : `s` with lowercase ASCII characters changed to uppercase

:see
>string
upper

:capy-status cpp-specific

:content
`to_upper()` is a C++ API. It normalizes ASCII text. It does not convert Unicode case.

Capy does not expose `to_upper`. Use `upper(value)`.

:example cpp render
print(to_upper("bearer"), "\n");
