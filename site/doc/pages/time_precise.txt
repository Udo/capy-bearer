:sig
f64 time_precise()

:returns
current Unix timestamp as an `f64`

:see
>time
usleep
time
time_format_local
time_format_relative
time_format_utc
time_parse
:content
Call `time_precise()` to get the current Unix timestamp as an `f64`.

Use this value to measure elapsed time within an operation. C++ uses the same no-input call and return value.


:example capy render
if time_precise() > 0.0 { print("timer\n") } else { print("no timer\n") }

:example cpp render
print(time_precise() > 0 ? "timer\n" : "no timer\n");
