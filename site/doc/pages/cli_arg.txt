:sig
`cli_arg` is not available in Capy.

:content
Capy does not provide the retired CLI argument reader.

In a `CLI` handler, read the required value from `request.query`, `request.form`, or `request.body`. Select the field that matches the input source.

:see
>1_CLI
>0_Request
cli_input
