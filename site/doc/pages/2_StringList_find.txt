:sig
DValue::find(String predicate, String fallback = "") (legacy `StringList` page)

:content
Removed as a separate StringList API. List-shaped `DValue::find()` returns the first matching string or the fallback.

:see
0_DValue
