:sig
function task_status(id : string) dval

:returns
Task status map.

:content
Use `task_status` to read a task status.

:example capy render
var status := task_status("")
print(string(status["state"]), "\n")
