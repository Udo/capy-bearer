:sig
function sqlite_error(handle : u64) string

:returns
Latest SQLite error text.

:content
Use `sqlite_error` after a SQLite operation when you need its diagnostic text.

:example capy render
var db := sqlite_connect(":memory:")
sqlite_query(db, "select * from missing_table")
print(sqlite_error(db), "\n")
sqlite_disconnect(db)
