:sig
function task(target : string, props : as dval) string

:returns
Task identifier.

:content
Use `task` to submit a task handler. Pass a unit path and a `dval` value for its input.

:example capy render
var id := task("/tests/task-handlers.capy", dval({"nested": {"value": "Ada"}}))
print(id, "\n")
