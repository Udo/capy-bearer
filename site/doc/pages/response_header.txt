:sig
function response_header(name : string, value : string)

:params
name : header name
value : header value

:content
`response_header()` sets a response header.

:example capy render
response_header("X-Mode", "docs")
