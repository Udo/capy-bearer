:sig
String route_path_sanitize(String path, String default_path = "index")

:see
>uri
route_path_is_safe
route_path_normalize
request_query_path
request_query_route
request_context_params

:content
In Capy, use this function before you compose a file path from a route.

It removes outer whitespace and leading and trailing `/`. Empty input becomes `default_path`. Each segment must be non-empty and contain only ASCII letters, digits, `_`, and `-`. The function rejects `.` and `..` segments. It returns an empty string for unsafe input.

`request_query_path()`, `request_query_route()`, and `ROUTE_PATH` already use this sanitizer.

:example capy render
print(route_path_sanitize("../secret", "index"), "\n")

:example cpp render
print(route_path_sanitize("../secret", "index"), "\n");
