:sig
function float_val(raw : string) f64

:params
raw : text that contains a number

:returns
The parsed floating-point value. Returns `0` if no usable number exists.

:content
`float_val` reads a floating-point value from text.

:example capy render
print(float_val("3.14"), "\n")
