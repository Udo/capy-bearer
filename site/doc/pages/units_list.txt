:sig
function units_list() dval

:returns
a `dval` list of unit paths

:see
>runtime
unit_info
unit_compile
unit_load

:content
`units_list` returns known unit paths.

:example capy render
for path := units_list() {
    print(path, "\n")
}
