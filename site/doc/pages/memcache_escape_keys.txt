:sig
DValue memcache_escape_keys(DValue keys)

:params
keys : input `dval` values
return value : new `dval` list of escaped keys

:see
>sys
>memcache_escape_key

:content
In Capy, this function converts each input item to a string and returns a new `dval` list.

It applies `memcache_escape_key()` to every item. The C++ API uses the same `DValue` input and result model.

:example capy render
var keys := dval(["a b", "c\td"])
print(join(memcache_escape_keys(keys), ", "), "\n")

:example cpp render
DValue keys;
keys.push_back("a b");
keys.push_back("c\td");
print(join(memcache_escape_keys(keys), ", "), "\n");
