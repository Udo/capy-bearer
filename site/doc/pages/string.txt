:sig
function string(value : dval, fallback : string = "") string

:returns
Converted text, or `fallback`.

:content
Use `string` to convert a `dval` value to text. It returns `fallback` when conversion fails.

:example capy render
print(string(dval(42)), "\n")
