#exports echo, default_input, VersionInfo

struct VersionInfo { name : string }

function echo(input : dval) dval {
    -> input
}

function default_input(input : dval) dval {
    -> dval("default")
}
