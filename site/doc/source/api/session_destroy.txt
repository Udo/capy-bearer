:sig
function session_destroy(name : string = "bearer-session") dval

:params
name : session cookie name

:returns
The resulting session state.

:content
`session_destroy()` expires the named session cookie and removes its server-side data.

:example capy render
var session := session_destroy("docs-session")
print(string(session.id, ""), "\n")
