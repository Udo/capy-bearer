:sig
function session_remove(key : string) dval

:params
key : session field name

:returns
The updated session state.

:content
Call `session_start()` before you remove a session field.

:example capy render
var session := session_start("docs-session")
session = session_remove("theme")
print(string(session.name, ""), "\n")
