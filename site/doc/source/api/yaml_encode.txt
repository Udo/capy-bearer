:sig
function yaml_encode(value : dval) string

:params
value : value to encode

:returns
The YAML source.

:content
`yaml_encode()` encodes a `dval` as YAML.

:example capy render
print(yaml_encode(dval({name: "Ada"})))

:see-group
encoding

:see
type/dvalue
