:sig
function ascii_safe_name(raw : string) string

:params
raw : source text

:returns
an ASCII-safe name

:content
`ascii_safe_name` converts text to an ASCII-safe name.

Use it when a target accepts ASCII names only.

:example capy render
print(ascii_safe_name("Café file-1"), "\n")

:see
safe_name
