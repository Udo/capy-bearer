:sig
function session_start(name : string = "bearer-session") dval

:params
name : session cookie name

:returns
The current session state.

:content
`session_start()` restores the named session or creates one.

:example capy render
var session := session_start("docs-session")
print(string(session.id), "\n")
