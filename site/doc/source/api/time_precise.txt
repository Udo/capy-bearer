:sig
function time_precise() f64

:returns
current Unix timestamp with fractional seconds

:content
`time_precise` returns the current Unix timestamp with fractional seconds.

:example capy render
print(time_precise(), "\n")

:see-group
time
