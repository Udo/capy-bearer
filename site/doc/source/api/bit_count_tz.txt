:sig
function bit_count_tz(value : s8) s64
function bit_count_tz(value : s16) s64
function bit_count_tz(value : s32) s64
function bit_count_tz(value : s64) s64
function bit_count_tz(value : u8) s64
function bit_count_tz(value : u16) s64
function bit_count_tz(value : u32) s64
function bit_count_tz(value : u64) s64

:params
value : the integer value

:returns
An `s64` count.

:content
`bit_count_tz()` counts trailing zero bits in the width of `value`. An `u8` value uses eight bits, not its container width.

The compiler emits a direct lib call.

:example capy render
var value := bit_count_tz(u8(1))
print(value, "\n")

:see-group
bits
