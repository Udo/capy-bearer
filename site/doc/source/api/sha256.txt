:sig
function sha256(value : string) string

:returns
Raw 32-byte SHA-256 digest.

:content
Use `sha256` to create a raw SHA-256 digest. Use `sha256_hex` when you need hexadecimal text.

:example capy render
print(length(sha256("abc")), "\n")
