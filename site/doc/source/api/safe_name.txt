:sig
function safe_name(raw : string) string

:params
raw : source text

:returns
Text that contains only letters, digits, and `_`.

:content
`safe_name()` removes all other characters. The result can be empty.

:example capy render
print(safe_name("Capy docs-2024"), "\n")
